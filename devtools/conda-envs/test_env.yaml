name: openff-utilities-test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - importlib_resources

    # Testing
  - pytest
  - pytest-cov
  - mypy =1.6
  - types-setuptools
  - types-toml
