name: openff-utilities-test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
    # Needed for Python <=3.8
  - importlib_resources

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - mypy
  - types-setuptools
  - types-toml
